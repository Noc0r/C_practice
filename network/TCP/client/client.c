#include "client.h"

static bool create_socket(client interface)
{
}

static bool connect(client interface, SA *server)
{
}

static int send(client interface)
{
}

static int read(client interface)
{
}

static int get_socket(client interface)
{
}

static void set_socket(client interface)
{
}

static client_errors get_last_error()
{

}

client* create_client()
{

}